app.controller("ctrlModulos", function ($scope, $http) {

    //var n_usuario = document.getElementById('n_usuario').value;
    /*$scope.n_mesa = '';

    $scope.loadProcesos = function () {
        $http.get(C_SERVER + "modulos/registro_credencial/buscar_mesa/" + $scope.n_mesa).
                success(function (data, status, headers, config) {
                    alert(1);
                    for (x = 0; x < data.length; x++) {
                        $scope.dataProcesos[x] = data[x];
                    }
                });
    };*/      

});
